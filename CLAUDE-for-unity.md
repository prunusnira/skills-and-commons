# CLAUDE.md

이 파일은 Claude Code가 Unity 프로젝트에서 작업할 때 참고하는 일반 가이드입니다.

## 작업 디렉터리 원칙

- 별도의 Git worktree를 생성하거나 다른 worktree로 전환하지 않는다.
- 사용자가 현재 열어 둔 작업 디렉터리에서 직접 작업하여 로컬 변경사항을 바로 확인할 수 있게 한다.
- 현재 작업 디렉터리에 있는 기존 로컬 변경사항은 유지하며, 요청과 무관한 변경을 수정하거나 되돌리지 않는다.

## 1. 작업 전 확인

- `ProjectSettings/ProjectVersion.txt`에서 Unity 버전을 확인한다.
- `Packages/manifest.json`과 `Packages/packages-lock.json`에서 패키지 및 버전을 확인한다.
- `Assets/`, `ProjectSettings/`, `Packages/` 구조와 기존 구현 패턴을 먼저 살핀다.
- 파일 경로와 타입 이름은 검색하여 실제 존재 여부를 확인하고 추정하지 않는다.
- 저장소에 별도 지침이나 테스트·빌드 스크립트가 있으면 그 지침을 우선한다.

## 2. 일반적인 프로젝트 구조

```text
Assets/             게임 코드와 에셋
ProjectSettings/    Unity 프로젝트 설정
Packages/           UPM 패키지 설정
```

`Assets/` 아래의 세부 구조는 프로젝트마다 다르므로 기존 구성을 따른다. 새 폴더나 계층은 필요할 때만 추가한다.

Unity가 생성하는 `Library/`, `Temp/`, `Logs/`, `obj/`, `.csproj`, `.sln` 파일은 직접 수정하거나 커밋하지 않는다.

## 3. 코드 작성 원칙

- 변경 범위를 요청 사항에 한정하고 불필요한 추상화나 선행 설계를 추가하지 않는다.
- 파일명은 주 타입명과 일치시키고 주변 코드의 네임스페이스 및 포맷을 따른다.
- 인스펙터 노출에는 일반적으로 `public` 필드보다 `[SerializeField] private`를 사용한다.
- 상수나 설정값으로 표현할 수 있는 매직 넘버와 문자열을 코드에 반복하지 않는다.
- 사용하지 않는 코드, 변수, `using`을 남기지 않는다.
- 주석은 코드만으로 이유를 알기 어려운 경우에만 간결하게 작성한다.
- `dynamic`과 불필요한 박싱·캐스팅을 피하고 구체적인 타입을 사용한다.
- 외부 파일, 네트워크 응답, 사용자 입력 등 신뢰할 수 없는 데이터는 검증한 뒤 사용한다.

### Unity 성능

- `Update`, `FixedUpdate`, 빈번한 콜백에서 LINQ, 문자열 결합, 반복적인 컬렉션 생성 등 불필요한 할당을 피한다.
- 반복적으로 사용하는 컴포넌트 참조는 적절히 캐시한다.
- 물리 처리는 `FixedUpdate`, 프레임 기반 입력과 화면 갱신은 용도에 맞는 Unity 생명주기를 사용한다.
- 최적화는 추측보다 프로파일링 결과와 실제 호출 빈도를 근거로 한다.

## 4. 에셋과 직렬화

- `.scene`, `.prefab`, `.asset`은 직렬화 형식과 참조 관계를 확인한 뒤 수정한다.
- 텍스트 편집으로 참조가 손상될 위험이 있으면 Unity 에디터에서 작업하도록 안내한다.
- 에셋을 이동하거나 이름을 바꿀 때 `.meta`의 GUID가 유지되도록 Unity 에디터 또는 GUID를 보존하는 방식을 사용한다.
- `.meta` 파일을 임의로 생성하거나 GUID를 직접 변경하지 않는다.
- 직렬화된 필드의 이름이나 타입 변경은 기존 씬·프리팹 데이터 유실 가능성을 검토한다.

## 5. 작업 절차

1. 관련 파일과 호출 경로를 읽어 현재 동작을 파악한다.
2. 코드베이스에 같은 문제를 해결한 기존 패턴이 있는지 검색한다.
3. 가장 작은 범위로 변경한다.
4. 컴파일 오류, 누락된 참조, 직렬화 영향과 관련 테스트를 확인한다.
5. 변경된 파일만 검토하고 요청과 무관한 수정이 섞이지 않았는지 확인한다.

새 파일을 만들기 전 유사한 역할의 파일이 있는지 확인한다. 기존 파일을 수정할 때는 일부 문맥만 보고 판단하지 말고 관련 타입과 사용처까지 확인한다.

## 6. 빌드와 테스트

- 프로젝트에 정의된 빌드·테스트 방법을 우선 사용한다.
- 자동화가 없다면 Unity 에디터의 Console, Test Runner, 대상 씬 실행으로 검증한다.
- CLI 테스트가 구성되어 있으면 Unity의 batch mode와 Test Framework 설정을 따른다.
- EditMode 테스트는 순수 로직과 에디터 기능에, PlayMode 테스트는 생명주기·씬·프레임 상호작용에 사용한다.
- 테스트를 실행할 수 없으면 실행하지 못한 이유와 수동 검증 항목을 명시한다.

## 7. 확인이 필요한 변경

다음처럼 결과가 크게 달라지거나 되돌리기 어려운 작업은 구현 전에 사용자에게 확인한다.

- 씬, 프리팹, 애니메이션, 렌더링 구조를 크게 바꾸는 경우
- 기존 컴포넌트 수정과 신규 컴포넌트 도입 사이에 명확한 근거가 없는 경우
- 네트워크 권한이나 동기화 방식을 결정해야 하는 경우
- 저장 데이터, `ScriptableObject`, 외부 데이터 스키마를 변경하는 경우
- 게임 규칙, 난이도, 타이밍, 수치 균형에 영향을 주는 경우
- 요구 사항과 기존 구현이 충돌하거나 여러 해석이 가능한 경우

기존 패턴과 요구 사항으로 판단이 명확하고 변경이 쉽게 되돌릴 수 있으면 확인 없이 진행할 수 있다.

## 8. Git

- 커밋과 푸시는 명시적으로 요청받은 경우에만 수행한다.
- 사용자 변경과 관련 없는 파일은 수정하거나 되돌리지 않는다.
- Unity 생성 파일과 로컬 전용 설정은 커밋하지 않는다.
- `--force`, `--no-verify` 같은 우회 옵션은 사용하지 않는다.
- 커밋 메시지와 브랜치 이름은 저장소의 기존 규칙을 따른다.
