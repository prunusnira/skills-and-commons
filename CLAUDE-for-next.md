# CLAUDE.md

이 문서는 Next.js 프로젝트에서 코드를 분석·수정·작성할 때 따라야 할 기본 규칙을 정의한다.

프로젝트에 이미 존재하는 규칙, 설정, 디렉터리 구조가 이 문서와 충돌한다면 기존 프로젝트의 규칙을 우선한다.

---

## 작업 디렉터리 원칙

* 별도의 Git worktree를 생성하거나 다른 worktree로 전환하지 않는다.
* 사용자가 현재 열어 둔 작업 디렉터리에서 직접 작업하여 로컬 변경사항을 바로 확인할 수 있게 한다.
* 현재 작업 디렉터리에 있는 기존 로컬 변경사항은 유지하며, 요청과 무관한 변경을 수정하거나 되돌리지 않는다.

---

## 1. 기본 원칙

* 응답과 설명은 한국어로 작성한다.
* 요청된 범위를 벗어나는 수정은 하지 않는다.
* 기존 코드 스타일과 설계 방향을 우선한다.
* 문제를 해결하기 전에 관련 코드를 충분히 확인한다.
* 확인하지 않은 내용을 추측하여 구현하지 않는다.
* 불확실한 부분은 불확실하다고 명시한다.
* 변경 범위는 가능한 작게 유지한다.
* 동작에 필요하지 않은 리팩터링을 함께 수행하지 않는다.
* 새로운 패턴이나 라이브러리를 도입하기 전에 기존 구현을 먼저 찾는다.
* 기존 구현이 정상적으로 동작하고 있다면 명확한 이유 없이 교체하지 않는다.

---

## 2. 작업 시작 전 확인

코드를 수정하기 전에 다음 항목을 확인한다.

* `package.json`
* 사용 중인 Next.js 버전
* 사용 중인 React 버전
* 패키지 매니저와 lock 파일
* `next.config.*`
* `tsconfig.json`
* ESLint 설정
* 테스트 설정
* 대상 코드와 인접한 코드
* 동일한 기능을 구현한 기존 코드
* 프로젝트 디렉터리 구조
* App Router 또는 Pages Router 사용 여부
* `cacheComponents` 등 렌더링 및 캐시 관련 설정

확인하지 않은 패키지나 API가 설치되어 있다고 가정하지 않는다.

패키지 매니저는 lock 파일을 기준으로 판단한다.

* `pnpm-lock.yaml`: pnpm
* `yarn.lock`: Yarn
* `package-lock.json`: npm
* `bun.lock` 또는 `bun.lockb`: Bun

프로젝트에서 사용 중인 패키지 매니저를 임의로 변경하지 않는다.

---

## 3. Next.js 라우터 규칙

### 3.1 기존 라우터 유지

* 새 프로젝트에서는 별도 요구가 없다면 App Router를 기준으로 한다.
* 기존 프로젝트가 Pages Router를 사용한다면 요청 없이 App Router로 이전하지 않는다.
* App Router와 Pages Router를 불필요하게 혼용하지 않는다.
* 라우터 이전은 별도의 명시적인 작업으로 취급한다.

### 3.2 App Router 파일 규칙

Next.js의 파일 규칙을 우선한다.

* `page.tsx`: 라우트 페이지
* `layout.tsx`: 공유 레이아웃
* `loading.tsx`: 로딩 UI
* `error.tsx`: 라우트 단위 예외 처리
* `global-error.tsx`: 루트 전역 예외 처리
* `not-found.tsx`: 404 UI
* `route.ts`: Route Handler
* `template.tsx`: 이동 시 새로 생성되어야 하는 레이아웃
* `default.tsx`: Parallel Route의 기본 UI

일반 컴포넌트에 Next.js 예약 파일명을 사용하지 않는다.

### 3.3 라우트 구조

* URL 구조와 기능 구조가 자연스럽게 대응되도록 구성한다.
* URL에 노출될 필요가 없는 폴더는 Route Group 또는 private folder를 검토한다.
* Route Group은 URL이 아니라 코드 구조를 정리하기 위한 목적으로 사용한다.
* 과도하게 깊은 라우트 중첩을 만들지 않는다.
* 하나의 `page.tsx`에 모든 로직을 작성하지 않는다.
* 페이지는 데이터 조합과 화면 구성을 담당하고, 세부 UI와 로직은 적절히 분리한다.

---

## 4. Server Component와 Client Component

### 4.1 Server Component 우선

App Router에서는 Server Component를 기본으로 사용한다.

다음 기능이 필요하지 않다면 `'use client'`를 추가하지 않는다.

* `useState`, `useReducer`
* `useEffect`, `useLayoutEffect`
* 이벤트 핸들러
* 브라우저 API
* 클라이언트 전용 Context
* 클라이언트 전용 라이브러리
* `usePathname`, `useSearchParams` 등 클라이언트 전용 훅

다음 작업은 가능한 Server Component에서 수행한다.

* 데이터 조회
* 권한 확인
* 서버 환경변수 접근
* 데이터 가공
* 정적인 콘텐츠 렌더링
* SEO에 필요한 콘텐츠 구성

### 4.2 Client Component 경계 최소화

* `'use client'`는 필요한 가장 하위 컴포넌트에 배치한다.
* 페이지나 레이아웃 전체를 단순 편의를 위해 Client Component로 만들지 않는다.
* 상호작용이 필요한 작은 영역만 Client Component로 분리한다.
* Client Component가 Server Component를 직접 import하는 구조를 만들지 않는다.
* 필요한 경우 Server Component에서 Client Component를 조합하고 `children`으로 전달한다.
* 서버 전용 코드가 클라이언트 번들에 포함되지 않도록 주의한다.

### 4.3 경계 간 데이터 전달

Server Component에서 Client Component로 전달하는 값은 직렬화 가능한 형태로 제한한다.

* 문자열
* 숫자
* 불리언
* `null`
* 배열
* 일반 객체
* 직렬화 가능한 값으로 구성된 데이터

서버 전용 객체, 데이터베이스 연결 객체, 함수 등을 Client Component의 props로 전달하지 않는다.

Client Component에 전달하는 데이터는 필요한 필드만 포함한다.

---

## 5. 컴포넌트 설계

### 5.1 책임 분리

* 하나의 컴포넌트는 하나의 명확한 역할을 갖도록 한다.
* 페이지 컴포넌트가 세부 UI 구현까지 모두 담당하지 않도록 한다.
* 데이터 조회, 상태 관리, 표현 로직을 무조건 한 파일에 모으지 않는다.
* 단순히 파일을 줄이기 위한 의미 없는 컴포넌트 분리는 하지 않는다.
* 한 번만 사용되는 짧은 마크업을 반드시 별도 컴포넌트로 만들 필요는 없다.

### 5.2 컴포넌트 이름

* React 컴포넌트는 PascalCase를 사용한다.
* 훅은 `use`로 시작한다.
* 이벤트 핸들러는 동작이 드러나는 이름을 사용한다.
* 불리언은 상태가 드러나는 이름을 사용한다.

```tsx
const isLoading = false;
const hasNextPage = true;
const canSubmit = true;

const handleSubmit = () => {};
const handleItemSelect = () => {};
```

다음과 같이 의미가 불명확한 이름은 피한다.

```tsx
const flag = false;
const data2 = [];
const handle = () => {};
```

### 5.3 Props

* Props 타입은 명시한다.
* 사용하지 않는 Props를 미리 추가하지 않는다.
* 지나치게 많은 불리언 Props 조합을 만들지 않는다.
* 상태 종류가 명확하다면 union 타입을 사용한다.
* 단순한 Props 전달을 피하기 위해 무조건 Context를 만들지 않는다.
* DOM 요소의 Props를 확장할 때는 React의 기본 타입을 활용한다.

```tsx
type ButtonProps = React.ComponentPropsWithoutRef<'button'> & {
    variant?: 'primary' | 'secondary';
};
```

### 5.4 합성 우선

* 거대한 설정 객체 하나로 모든 UI를 제어하는 방식보다 컴포넌트 합성을 우선 검토한다.
* `children`과 명시적인 slot Props를 적절히 사용한다.
* 상속 기반 컴포넌트 구조는 사용하지 않는다.

---

## 6. React 작성 규칙

### 6.1 렌더링 순수성

렌더링 중에는 부수 효과를 발생시키지 않는다.

금지되는 예:

* 렌더링 중 상태 변경
* 렌더링 중 네트워크 요청 실행
* 렌더링 중 DOM 직접 조작
* 렌더링 중 전역 변수 변경
* 렌더링 결과에 영향을 주는 임의의 `Date.now()` 또는 `Math.random()` 호출

부수 효과는 이벤트 핸들러, Server Action 또는 필요한 Effect에서 처리한다.

### 6.2 Hook 규칙

* Hook은 컴포넌트 또는 커스텀 Hook의 최상위에서만 호출한다.
* 조건문, 반복문, 중첩 함수 내부에서 Hook을 호출하지 않는다.
* Hook 호출 순서가 렌더링마다 달라지지 않도록 한다.
* Effect 의존성 경고를 무시하지 않는다.
* 의존성 배열을 비우기 위해 ESLint 규칙을 비활성화하지 않는다.

### 6.3 Effect 최소화

`useEffect`는 외부 시스템과 React 상태를 동기화할 때 사용한다.

적절한 사용 예:

* 브라우저 이벤트 구독
* 타이머 등록
* 외부 라이브러리 인스턴스 동기화
* DOM API 연동
* WebSocket 연결

다음 작업에는 Effect를 우선 사용하지 않는다.

* Props로부터 파생 상태 계산
* 배열 필터링 및 정렬
* 사용자 이벤트 처리
* 다른 상태가 변경됐다는 이유만으로 상태 복사
* 렌더링에 필요한 단순 계산

```tsx
// 지양
const [fullName, setFullName] = useState('');

useEffect(() => {
    setFullName(`${firstName} ${lastName}`);
}, [firstName, lastName]);

// 권장
const fullName = `${firstName} ${lastName}`;
```

### 6.4 메모이제이션

* `useMemo`, `useCallback`, `memo`를 습관적으로 사용하지 않는다.
* 실제 성능 문제나 참조 안정성이 필요한 경우에만 사용한다.
* 메모이제이션 적용 전 계산 비용과 리렌더링 원인을 확인한다.
* 단순한 함수 또는 값이라는 이유만으로 메모이제이션하지 않는다.
* 프로젝트에서 React Compiler를 사용한다면 해당 설정과 규칙을 우선한다.

### 6.5 상태 관리

상태는 필요한 가장 가까운 위치에 둔다.

상태 종류에 따라 다음 순서로 검토한다.

1. 서버에서만 필요한 값
2. URL로 표현해야 하는 값
3. 컴포넌트 로컬 상태
4. 상위 컴포넌트로 끌어올린 상태
5. Context
6. 프로젝트의 전역 상태 관리 도구

검색 조건, 필터, 페이지 번호처럼 공유하거나 새로고침 후 유지되어야 하는 상태는 URL 상태를 우선 검토한다.

서버에서 받은 데이터를 이유 없이 클라이언트 전역 상태에 복사하지 않는다.

---

## 7. 데이터 조회

### 7.1 서버 데이터 조회 우선

가능하다면 Server Component에서 데이터를 조회한다.

* 서버에서 직접 `fetch` 또는 서버 전용 데이터 접근 계층을 사용한다.
* 데이터베이스 또는 내부 SDK에 접근할 수 있다면 불필요한 내부 HTTP 요청을 만들지 않는다.
* Server Component에서 같은 애플리케이션의 Route Handler를 다시 호출하지 않는다.
* 클라이언트에 필요하지 않은 데이터는 서버에 유지한다.

### 7.2 병렬 조회

서로 의존하지 않는 요청은 병렬로 처리한다.

```tsx
const [user, products] = await Promise.all([
    getUser(),
    getProducts(),
]);
```

다음과 같이 불필요한 waterfall을 만들지 않는다.

```tsx
const user = await getUser();
const products = await getProducts();
```

두 번째 요청이 첫 번째 결과에 의존한다면 순차 요청을 사용한다.

### 7.3 클라이언트 데이터 조회

다음과 같은 경우 클라이언트 조회를 검토한다.

* 사용자 상호작용 이후에만 필요한 데이터
* 실시간 또는 주기적 갱신
* 브라우저 API에 의존하는 요청
* 클라이언트 캐시 라이브러리가 필요한 화면
* 무한 스크롤 및 사용자 조작 중심의 목록

프로젝트에 React Query, SWR 등 기존 도구가 있다면 해당 도구를 우선 사용한다.

동일한 목적의 데이터 조회 라이브러리를 추가로 도입하지 않는다.

### 7.4 데이터 접근 계층

복잡한 프로젝트에서는 데이터 접근 로직을 UI와 분리한다.

예시:

```text
features/
  product/
    api/
    components/
    model/
    utils/
```

또는 프로젝트에서 이미 사용하는 구조를 따른다.

다음 로직은 페이지 JSX 내부에 직접 길게 작성하지 않는다.

* API 응답 변환
* 복잡한 필터링
* 권한별 데이터 가공
* 도메인 규칙
* 반복되는 요청 옵션

---

## 8. 캐시와 재검증

Next.js의 캐시 동작은 버전과 프로젝트 설정에 따라 달라질 수 있다.

캐시 관련 코드를 수정하기 전에 반드시 다음 항목을 확인한다.

* Next.js 버전
* `next.config.*`
* `cacheComponents` 활성화 여부
* 기존 `fetch` 옵션
* 기존 캐시 지시어
* `revalidatePath`, `revalidateTag` 사용 여부
* Route Segment 설정
* 배포 환경

캐시 동작을 기본값에 의존하여 추측하지 않는다.

데이터 특성에 따라 캐시 정책을 명시적으로 결정한다.

* 요청마다 최신 데이터가 필요한가
* 일정 시간 동안 재사용 가능한가
* 사용자별 데이터인가
* 권한에 따라 달라지는가
* 변경 시 즉시 무효화해야 하는가
* 경로 단위 또는 태그 단위 무효화가 필요한가

사용자별 또는 인증별 데이터를 공용 캐시에 저장하지 않도록 주의한다.

변경 작업 후 필요한 캐시 무효화가 누락되지 않았는지 확인한다.

캐시를 비활성화하여 문제를 우회하기 전에 기존 캐시 설계와 성능 영향을 확인한다.

---

## 9. 데이터 변경

데이터 생성, 수정, 삭제는 서버에서 검증하고 처리한다.

App Router에서는 프로젝트 상황에 따라 다음 방식을 사용한다.

* Server Action 또는 Server Function
* Route Handler
* 외부 API 서버 호출
* 프로젝트에서 사용하는 mutation 라이브러리

Server Action을 사용하는 경우:

* 서버 전용 파일임을 명확히 한다.
* 입력값을 서버에서 다시 검증한다.
* 인증과 권한을 서버에서 확인한다.
* 클라이언트에서 전달된 ID와 권한 정보를 신뢰하지 않는다.
* 변경 성공 후 필요한 캐시를 무효화한다.
* 예상 가능한 오류와 예상하지 못한 오류를 구분한다.
* 서버 내부 오류 정보를 그대로 사용자에게 노출하지 않는다.

클라이언트 검증은 사용자 경험을 위한 것이며 서버 검증을 대체하지 않는다.

---

## 10. Route Handler

Route Handler는 다음 용도로 사용한다.

* 외부 클라이언트에 제공하는 HTTP API
* Webhook 수신
* 인증 callback
* 파일 또는 스트림 응답
* BFF API
* 브라우저에서 호출해야 하는 서버 엔드포인트

Server Component가 내부 데이터를 가져오기 위한 중간 계층으로 Route Handler를 만들지 않는다.

Route Handler 작성 시:

* HTTP 메서드를 명확히 구분한다.
* 요청 본문과 query parameter를 검증한다.
* 인증과 권한을 확인한다.
* 적절한 HTTP 상태 코드를 반환한다.
* 내부 예외 메시지와 stack trace를 노출하지 않는다.
* 응답 타입과 에러 형식을 가능한 일관되게 유지한다.
* CORS가 필요하지 않다면 불필요한 허용 헤더를 추가하지 않는다.

```tsx
import { NextResponse } from 'next/server';

export async function GET() {
    return NextResponse.json({ data: [] });
}
```

---

## 11. 타입스크립트

### 11.1 기본 규칙

* 가능한 엄격한 타입을 유지한다.
* `any` 사용을 피한다.
* 외부 입력은 `unknown`으로 받은 뒤 검증한다.
* 타입 단언보다 타입 좁히기를 우선한다.
* 사용되지 않는 타입과 import를 남기지 않는다.
* 타입 전용 import는 `import type`을 사용한다.
* 함수의 반환 타입은 추론이 명확하지 않거나 공개 API인 경우 명시한다.
* 런타임 값이 필요한 경우 `enum`보다 객체와 union 타입을 우선 검토한다.

```tsx
export const ORDER_STATUS = {
    Pending: 'pending',
    Completed: 'completed',
    Cancelled: 'cancelled',
} as const;

export type OrderStatus =
    (typeof ORDER_STATUS)[keyof typeof ORDER_STATUS];
```

### 11.2 타입 재사용

* 실제 도메인 의미가 같은 타입만 재사용한다.
* 형태가 우연히 같다는 이유로 다른 도메인의 타입을 재사용하지 않는다.
* API 응답 타입과 화면 모델이 다르면 변환 계층을 둔다.
* 거대한 공용 타입 파일에 모든 타입을 모으지 않는다.
* 타입은 사용하는 기능과 가까운 위치에 둔다.

### 11.3 선택적 속성

`optional`과 `undefined`를 구분한다.

```tsx
type Example = {
    optionalValue?: string;
    explicitValue: string | undefined;
};
```

속성이 존재하지 않는 것과 속성 값이 `undefined`인 것이 다른 의미라면 이를 타입에 반영한다.

### 11.4 오류 억제 금지

다음 방식으로 타입 오류를 무조건 숨기지 않는다.

```tsx
// @ts-ignore
value as any
```

불가피하게 억제가 필요하면 이유를 확인하고 가능한 좁은 범위에서 처리한다.

---

## 12. 입력값 검증

다음 값은 신뢰하지 않는다.

* URL parameter
* Query string
* FormData
* Request body
* Cookie
* Header
* 외부 API 응답
* 로컬 스토리지 값

프로젝트에 Zod, Valibot 등의 검증 도구가 있다면 기존 도구를 사용한다.

검증 라이브러리가 없는 단순한 경우에는 불필요하게 새 의존성을 추가하지 않고 명시적인 타입 가드를 작성할 수 있다.

검증 실패 시:

* 사용자 입력 오류와 서버 오류를 구분한다.
* 사용자가 수정할 수 있는 메시지를 제공한다.
* 민감한 내부 정보를 노출하지 않는다.

---

## 13. 오류 처리

### 13.1 예상 가능한 오류

다음과 같은 오류는 예상 가능한 오류로 취급한다.

* 잘못된 사용자 입력
* 인증 실패
* 권한 부족
* 존재하지 않는 데이터
* 비즈니스 규칙 위반
* 중복 요청

예상 가능한 오류는 화면에서 처리 가능한 결과 형태로 반환한다.

```tsx
type ActionResult =
    | { success: true }
    | { success: false; message: string };
```

### 13.2 예상하지 못한 오류

다음 오류는 숨기지 않고 예외 경계 또는 로깅 시스템에서 확인할 수 있게 한다.

* 데이터베이스 장애
* 외부 API 장애
* 구현 오류
* 불가능한 상태
* 설정 누락

의미 없는 `try-catch`로 오류를 삼키지 않는다.

```tsx
// 지양
try {
    await execute();
} catch {
    return null;
}
```

오류를 처리할 수 없다면 상위 오류 경계가 처리하도록 다시 throw한다.

### 13.3 App Router 오류 UI

* 라우트 단위 오류는 `error.tsx`를 사용한다.
* 루트 전역 오류는 `global-error.tsx`를 검토한다.
* 찾을 수 없는 리소스는 `notFound()`와 `not-found.tsx`를 사용한다.
* 리다이렉트는 `redirect()` 또는 `permanentRedirect()`를 사용한다.
* 정상적인 분기 처리를 예외 메시지 문자열 비교에 의존하지 않는다.

---

## 14. 로딩과 Suspense

* 데이터 조회 시간이 긴 영역은 적절한 로딩 UI를 제공한다.
* 전체 페이지를 하나의 거대한 로딩 경계로 감싸지 않는다.
* 독립적으로 표시할 수 있는 영역은 별도 Suspense 경계를 검토한다.
* 레이아웃 이동이 크지 않은 skeleton을 사용한다.
* 로딩 UI가 실제 콘텐츠 크기와 지나치게 다르지 않도록 한다.
* 무조건 Suspense를 추가하지 않고 스트리밍 효과가 있는 위치에 사용한다.

---

## 15. 폼

* 단순한 폼은 HTML 기본 동작과 서버 처리를 우선 검토한다.
* 클라이언트 상태가 필요하지 않은 모든 입력을 `useState`로 관리하지 않는다.
* 프로젝트에 React Hook Form 등 기존 폼 도구가 있다면 이를 따른다.
* 서버에서 입력값을 다시 검증한다.
* 제출 중 중복 요청을 방지한다.
* 성공, 실패, 진행 중 상태를 사용자에게 전달한다.
* 오류 메시지는 관련 입력과 연결한다.
* 키보드만으로 폼을 사용할 수 있어야 한다.
* 버튼의 기본 `type`에 의존하지 않는다.

```tsx
<button type="submit">저장</button>
<button type="button">취소</button>
```

---

## 16. 스타일링

* 프로젝트에서 사용 중인 스타일 방식을 유지한다.
* Tailwind CSS, CSS Modules, CSS-in-JS 등을 임의로 혼용하지 않는다.
* 새로운 UI 라이브러리를 요청 없이 추가하지 않는다.
* 기존 디자인 토큰과 공통 컴포넌트를 우선 사용한다.
* 동일한 색상, 간격, 폰트 값을 여러 곳에 하드코딩하지 않는다.
* 전역 CSS는 실제 전역 스타일에만 사용한다.
* 컴포넌트 전용 스타일은 가능한 해당 컴포넌트와 가까운 위치에 둔다.
* `!important`로 우선순위 문제를 무조건 우회하지 않는다.
* 불필요하게 높은 `z-index`를 사용하지 않는다.
* 반응형 동작을 고려한다.
* hover에만 의존하여 핵심 정보를 전달하지 않는다.

Tailwind CSS를 사용하는 경우:

* 동적으로 조합되어 빌드 시 탐지되지 않는 클래스 문자열을 주의한다.
* 같은 클래스 조합이 반복될 때에만 공통화한다.
* 단순히 클래스 문자열을 줄이기 위한 추상화는 하지 않는다.
* 프로젝트에 클래스 병합 유틸리티가 있다면 기존 유틸리티를 사용한다.

---

## 17. 이미지와 폰트

### 17.1 이미지

일반적인 콘텐츠 이미지는 `next/image` 사용을 우선 검토한다.

* 의미 있는 `alt`를 제공한다.
* 장식용 이미지는 빈 `alt`를 사용한다.
* 원격 이미지는 허용된 도메인 또는 패턴을 설정한다.
* 레이아웃 이동을 방지하도록 크기 또는 비율을 지정한다.
* 첫 화면의 핵심 이미지에만 우선 로딩을 적용한다.
* 모든 이미지에 우선 로딩을 적용하지 않는다.
* 단순 아이콘은 프로젝트의 아이콘 시스템을 따른다.

`<img>`가 필요한 명확한 이유가 있다면 사용할 수 있다.

### 17.2 폰트

* 프로젝트에서 사용하는 폰트 로딩 방식을 유지한다.
* 새 프로젝트에서는 `next/font`를 우선 검토한다.
* 같은 폰트를 여러 컴포넌트에서 반복해 초기화하지 않는다.
* 필요한 font weight만 로드한다.
* 시스템 폰트로 충분한 경우 불필요한 웹 폰트를 추가하지 않는다.

---

## 18. Metadata와 SEO

공개 페이지는 필요한 metadata를 제공한다.

* `title`
* `description`
* canonical URL
* Open Graph 정보
* 필요한 경우 Twitter 카드
* robots 정책

정적 metadata는 `metadata`를 사용하고, 데이터에 따라 달라지는 metadata는 `generateMetadata`를 사용한다.

* 같은 데이터를 페이지와 metadata에서 각각 불필요하게 중복 요청하지 않는다.
* 모든 페이지에 동일한 title을 사용하지 않는다.
* 사용자 입력을 검증 없이 metadata에 삽입하지 않는다.
* 비공개 페이지가 검색 엔진에 노출되지 않도록 확인한다.
* favicon과 OG 이미지는 Next.js 파일 규칙을 우선 검토한다.

---

## 19. 접근성

접근성은 선택 기능이 아니라 기본 요구사항으로 취급한다.

* 의미에 맞는 HTML 요소를 사용한다.
* 클릭 가능한 `div` 대신 `button` 또는 `a`를 사용한다.
* 모든 입력에 연결된 label을 제공한다.
* 키보드로 모든 핵심 기능을 사용할 수 있어야 한다.
* focus 표시를 제거하지 않는다.
* 모달이 열리면 focus 이동과 focus 복귀를 고려한다.
* 색상만으로 상태를 구분하지 않는다.
* 아이콘 버튼에는 접근 가능한 이름을 제공한다.
* 이미지에 적절한 `alt`를 제공한다.
* 제목 heading 순서를 의미에 맞게 구성한다.
* ARIA는 기본 HTML만으로 의미를 표현할 수 없을 때 사용한다.
* `aria-label`로 잘못된 HTML 구조를 감추지 않는다.

---

## 20. 보안

### 20.1 환경변수

* 비밀값은 서버에서만 사용한다.
* 클라이언트에 노출해도 되는 값만 `NEXT_PUBLIC_` 접두사를 사용한다.
* `NEXT_PUBLIC_` 값은 클라이언트 번들에 포함된다고 간주한다.
* `.env` 파일과 비밀키를 저장소에 커밋하지 않는다.
* 환경변수가 없을 때 조용히 잘못 동작하지 않도록 검증한다.

### 20.2 인증과 권한

* UI에서 버튼을 숨기는 것은 권한 검사가 아니다.
* 데이터 조회 및 변경 시 서버에서 권한을 확인한다.
* 클라이언트에서 전달한 사용자 ID나 역할을 신뢰하지 않는다.
* 보호된 데이터는 권한 확인 전에 조회하거나 반환하지 않는다.
* Redirect만으로 API 접근 제어를 대체하지 않는다.

### 20.3 외부 입력과 출력

* 사용자 입력을 검증한다.
* `dangerouslySetInnerHTML`은 불가피한 경우에만 사용한다.
* HTML을 출력해야 한다면 신뢰할 수 있는 방식으로 정화한다.
* 내부 오류, stack trace, SQL, 토큰을 응답에 노출하지 않는다.
* 외부 URL redirect는 허용 대상을 검증한다.
* 파일 업로드는 확장자만 신뢰하지 않는다.

---

## 21. 성능

성능 최적화는 측정 가능한 문제를 기준으로 수행한다.

우선 확인할 항목:

* 불필요한 Client Component 범위
* 큰 클라이언트 번들
* 순차적인 데이터 요청
* 중복 데이터 요청
* 과도한 리렌더링
* 큰 이미지
* 불필요한 third-party script
* 화면 밖 콘텐츠의 초기 렌더링
* 레이아웃 이동
* 느린 서버 요청

권장 원칙:

* 서버에서 처리할 수 있는 로직은 서버에 둔다.
* 무거운 클라이언트 전용 컴포넌트는 필요할 때 dynamic import를 검토한다.
* 독립적인 요청은 병렬화한다.
* 큰 목록은 pagination 또는 virtualization을 검토한다.
* 모든 컴포넌트에 메모이제이션을 적용하지 않는다.
* 번들 최적화는 실제 분석 결과를 기반으로 한다.
* 성능을 위해 가독성과 정확성을 과도하게 희생하지 않는다.

---

## 22. 디렉터리 구조

기존 프로젝트 구조를 우선한다.

새 구조가 필요한 경우 기능 단위 구성을 우선 검토한다.

```text
src/
  app/
  features/
    product/
      api/
      components/
      hooks/
      model/
      utils/
  components/
    ui/
  lib/
  styles/
```

각 디렉터리의 기본 역할:

* `app`: 라우팅, 레이아웃, 페이지 조합
* `features`: 도메인 또는 기능별 코드
* `components/ui`: 여러 기능에서 사용하는 표현 중심 UI
* `lib`: 특정 기능에 종속되지 않은 인프라 및 공통 설정
* `styles`: 전역 스타일과 공용 스타일 설정

다음 구조는 피한다.

* 역할이 불분명한 거대한 `utils` 디렉터리
* 모든 타입을 모은 거대한 `types.ts`
* 모든 API를 모은 단일 `api.ts`
* 기능과 무관하게 파일 종류만으로 지나치게 분리한 구조
* 실제 재사용되지 않는 공통 컴포넌트

파일을 공통 영역으로 이동하기 전에 실제로 여러 기능에서 같은 의미로 사용되는지 확인한다.

---

## 23. 유틸리티와 추상화

* 두세 줄의 단순 코드를 숨기기 위한 유틸리티를 무조건 만들지 않는다.
* 한 번만 사용되는 로직을 미래 재사용 가능성만으로 추상화하지 않는다.
* 이름만 다른 wrapper 함수를 만들지 않는다.
* 공통화 전에 코드의 형태뿐 아니라 의미가 같은지 확인한다.
* 추상화가 조건문과 옵션을 계속 늘린다면 기능별 구현 분리를 검토한다.
* 잘못된 추상화를 유지하기 위해 복잡한 타입을 만들지 않는다.

추상화는 다음 조건 중 하나를 만족할 때 검토한다.

* 동일한 도메인 규칙이 반복된다.
* 변경 시 여러 위치를 반드시 함께 수정해야 한다.
* 복잡한 구현 세부사항을 감출 필요가 있다.
* 테스트 가능한 독립 단위가 된다.

---

## 24. 주석과 문서

* 코드가 무엇을 하는지 그대로 설명하는 주석은 작성하지 않는다.
* 코드만으로 알기 어려운 이유와 제약을 설명한다.
* 임시 해결책에는 원인과 제거 조건을 작성한다.
* 외부 시스템의 제약으로 인한 코드라면 해당 이유를 남긴다.
* 주석으로 잘못된 코드 구조를 보완하지 않는다.
* 코드 변경 후 더 이상 맞지 않는 주석은 함께 수정하거나 제거한다.
* 요청하지 않은 장문의 JSDoc을 모든 함수에 추가하지 않는다.

```tsx
// 지양: 사용자 목록을 가져온다.
const users = await getUsers();

// 권장: 외부 API가 한 번에 최대 100개만 허용하므로 요청을 분할한다.
const users = await getUsersInBatches(ids);
```

---

## 25. 로깅

* 디버깅용 `console.log`를 최종 코드에 남기지 않는다.
* 운영 로그는 프로젝트의 기존 로깅 도구를 사용한다.
* 비밀번호, 토큰, Cookie, 개인정보를 로그에 기록하지 않는다.
* 동일한 오류를 여러 계층에서 중복 기록하지 않는다.
* 오류 로그에는 문제를 식별할 수 있는 문맥을 포함하되 민감한 값은 제외한다.
* 사용자에게 표시하는 메시지와 운영 로그 메시지를 구분한다.

---

## 26. 테스트

### 26.1 기본 원칙

* 구현 세부사항보다 사용자에게 보이는 동작을 검증한다.
* 테스트를 통과시키기 위해 실제 동작을 왜곡하지 않는다.
* 테스트마다 검증 목적을 명확하게 유지한다.
* 하나의 테스트에 서로 무관한 시나리오를 지나치게 많이 넣지 않는다.
* 기존 테스트 도구를 우선 사용한다.
* 요청 없이 테스트 프레임워크를 교체하거나 추가하지 않는다.

### 26.2 Given-When-Then

테스트는 가능한 Given-When-Then 구조로 작성한다.

```tsx
it('입력한 검색어로 상품을 조회한다', async () => {
    // Given
    const user = userEvent.setup();
    render(<SearchForm onSubmit={onSubmit} />);

    // When
    await user.type(screen.getByRole('textbox'), '키보드');
    await user.click(screen.getByRole('button', { name: '검색' }));

    // Then
    expect(onSubmit).toHaveBeenCalledWith('키보드');
});
```

### 26.3 검증 대상

우선 검증할 항목:

* 사용자 상호작용
* 조건에 따른 UI 변화
* 입력값 검증
* 권한별 동작
* 오류 및 로딩 상태
* 데이터 변환
* 비즈니스 규칙
* 접근 가능한 역할과 이름
* 회귀 가능성이 높은 동작

다음 테스트는 가치가 낮을 수 있으므로 목적을 확인한다.

* 컴포넌트가 단순히 render된다는 테스트
* 내부 state 값 직접 검증
* 구현 함수 호출 횟수만 검증
* 변경에 지나치게 민감한 대형 snapshot
* 프레임워크 자체 동작 검증
* 타입 시스템으로 이미 보장되는 내용 검증

### 26.4 Mock

* 실제 경계가 아닌 모든 코드를 무조건 mock하지 않는다.
* 네트워크, 시간, 브라우저 API, 외부 서비스 등 필요한 경계만 mock한다.
* 내부 구현 함수를 과도하게 mock하지 않는다.
* Mock이 실제 API 형태와 달라지지 않도록 관리한다.
* 테스트 간 mock 상태를 공유하지 않는다.

### 26.5 테스트 범위

변경 내용에 따라 적절한 테스트를 선택한다.

* 순수 함수와 도메인 규칙: 단위 테스트
* 컴포넌트 상호작용: 컴포넌트 테스트
* 여러 모듈 간 동작: 통합 테스트
* 핵심 사용자 흐름: E2E 테스트
* UI 회귀: Visual 테스트
* 접근성 기본 검증: Accessibility 테스트

모든 변경에 모든 종류의 테스트를 추가할 필요는 없다.

---

## 27. ESLint, TypeScript, 빌드 검증

작업 완료 후 프로젝트에 존재하는 명령어를 확인하여 가능한 범위에서 실행한다.

우선순위:

1. 변경 파일 관련 테스트
2. TypeScript 검사
3. ESLint
4. 전체 테스트
5. 프로덕션 빌드

명령어를 추측하지 않고 `package.json`의 scripts를 확인한다.

예시:

```bash
pnpm test
pnpm type-check
pnpm lint
pnpm build
```

모노레포에서는 변경 대상 패키지 또는 앱 범위로 실행할 수 있는 명령을 우선 사용한다.

```bash
pnpm --filter web lint
pnpm --filter web test
pnpm --filter web build
```

검증에 실패하면:

* 기존 오류인지 이번 변경으로 발생한 오류인지 구분한다.
* 관련 없는 기존 오류를 요청 없이 모두 수정하지 않는다.
* 실행하지 못한 검증은 실행한 것처럼 말하지 않는다.
* 실패 내용을 숨기지 않는다.

---

## 28. 의존성 관리

새 패키지를 추가하기 전에 확인한다.

* 기존 패키지로 해결 가능한가
* 플랫폼 API로 충분한가
* 프로젝트 내 유사 기능이 있는가
* 패키지 크기와 클라이언트 번들 영향은 어떤가
* 서버 전용으로 사용할 수 있는가
* 유지보수 상태와 호환성이 적절한가
* 현재 Next.js와 React 버전을 지원하는가

요청 없이 다음 작업을 하지 않는다.

* 패키지 매니저 변경
* lock 파일 전체 재생성
* 주요 버전 일괄 업데이트
* 여러 라이브러리 동시 교체
* npm, Yarn, pnpm 명령 혼용
* 이미 사용 중인 라이브러리와 같은 목적의 패키지 추가

패키지를 추가했다면 실제로 필요한 최소 범위에서 사용한다.

---

## 29. 모노레포

모노레포인 경우 다음을 확인한다.

* workspace 설정
* 루트와 앱별 `package.json`
* 패키지 경계
* 내부 패키지의 export
* TypeScript project reference 또는 path alias
* 빌드 도구 설정
* 패키지별 React 및 Next.js 버전
* peer dependency
* 서버 전용 코드의 클라이언트 패키지 유입 여부

공통 패키지를 수정할 때는 해당 패키지를 사용하는 다른 앱에 미치는 영향을 확인한다.

UI 패키지는 가능한 다음 특성을 유지한다.

* 특정 앱의 환경변수에 직접 의존하지 않는다.
* 특정 앱의 라우터 구조에 직접 의존하지 않는다.
* 서버 전용 코드와 클라이언트 코드를 명확히 분리한다.
* React를 중복 번들링하지 않도록 설정을 확인한다.
* 공개 API를 명확한 export로 제공한다.
* 내부 파일 경로를 직접 import하지 않는다.

---

## 30. 코드 수정 규칙

* 대상 기능과 관련된 파일만 수정한다.
* 요청 없이 파일명과 디렉터리를 대량 변경하지 않는다.
* 요청 없이 import 순서만 바꾸는 변경을 만들지 않는다.
* 포매터 때문에 관련 없는 파일 전체가 변경되지 않도록 한다.
* 공개 API 변경 시 사용처를 함께 확인한다.
* 함수 시그니처 변경 시 모든 호출부를 확인한다.
* 타입 변경 시 런타임 데이터 형태도 함께 확인한다.
* 삭제한 코드의 import, 타입, 테스트, 스타일을 정리한다.
* 기존 동작을 변경한다면 변경 의도가 테스트 또는 코드에서 드러나게 한다.

---

## 31. 금지되는 패턴

명확한 이유 없이 다음 패턴을 사용하지 않는다.

* 페이지 또는 루트 레이아웃 전체에 `'use client'` 추가
* 모든 데이터를 클라이언트에서 조회
* Server Component에서 내부 Route Handler 호출
* 사용자 입력을 검증 없이 사용
* 클라이언트에서만 권한 검사
* `any`로 타입 오류 우회
* 광범위한 타입 단언
* 빈 `catch` 블록
* 오류를 무조건 `null`로 변환
* 모든 함수에 `useCallback` 적용
* 모든 값에 `useMemo` 적용
* Props를 state로 불필요하게 복사
* 파생 값을 Effect로 동기화
* 배열 index를 안정적인 key처럼 사용
* 의미 없는 wrapper 컴포넌트 생성
* 미래 요구사항을 위한 과도한 추상화
* 요청하지 않은 의존성 추가
* 요청하지 않은 대규모 리팩터링
* 타입 검사 또는 ESLint 규칙 비활성화
* 민감한 환경변수에 `NEXT_PUBLIC_` 사용
* 디버깅용 로그와 주석 방치
* 테스트를 통과시키기 위한 실제 로직 왜곡

---

## 32. 작업 완료 기준

작업 완료 전 다음 항목을 확인한다.

### 기능

* 요청된 동작이 구현되었는가
* 기존 동작이 불필요하게 변경되지 않았는가
* 로딩, 오류, 빈 상태가 필요한가
* 엣지 케이스가 처리되었는가

### Next.js

* Server Component로 가능한 영역이 Client Component가 되지 않았는가
* `'use client'` 범위가 필요한 만큼만 설정되었는가
* 데이터 요청 waterfall이 발생하지 않는가
* 캐시 정책을 추측하지 않았는가
* 데이터 변경 후 재검증이 필요한가
* Route Handler가 불필요한 중간 계층으로 사용되지 않았는가

### 타입과 보안

* `any` 또는 과도한 타입 단언이 추가되지 않았는가
* 외부 입력을 검증했는가
* 서버에서 인증과 권한을 확인하는가
* 비밀값이 클라이언트에 노출되지 않는가
* 내부 오류 정보가 사용자에게 노출되지 않는가

### UI

* 키보드로 사용할 수 있는가
* 적절한 HTML 요소를 사용했는가
* 로딩 중 레이아웃 이동이 과도하지 않은가
* 모바일과 데스크톱에서 문제가 없는가
* 접근 가능한 이름과 label이 제공되는가

### 품질

* 관련 테스트를 추가하거나 수정했는가
* 변경 범위의 테스트가 통과하는가
* 타입 검사가 통과하는가
* ESLint가 통과하는가
* 불필요한 코드와 로그가 남아 있지 않은가
* 관련 없는 파일이 변경되지 않았는가

---

## 33. 응답 형식

코드 작업 결과를 설명할 때는 다음 내용을 중심으로 간결하게 작성한다.

* 변경한 내용
* 중요한 설계 판단
* 실행한 검증
* 남아 있는 문제 또는 확인하지 못한 사항

실행하지 않은 테스트나 빌드를 실행했다고 표현하지 않는다.

불필요한 작업 과정, 반복적인 요약, 형식적인 마무리 문장은 작성하지 않는다.
